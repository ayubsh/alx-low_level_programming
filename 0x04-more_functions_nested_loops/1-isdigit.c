/**
 * _isdigit - checks weather input is digit or not
 * @c: input to be checked
 * Return: 1 when c is digit otherwise 0
 */
int _isdigit(int c)
{
	if(c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
